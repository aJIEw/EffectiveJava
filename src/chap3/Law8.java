package chap3;

/**
 * 第8条：覆盖equals时请遵守通用约定（general contract）。
 *
 * 1）使用==检查参数是否为这个对象的引用
 * 2）使用instanceof检查参数是否为正确的类型
 * 3）把参数转换成正确的类型
 * 4）对于该类中的每个关键域（significant field），检查参数中的域是否与该对象中对应的域相匹配
 */
public class Law8 {

    public static void main(String[] args) {
    }

}
