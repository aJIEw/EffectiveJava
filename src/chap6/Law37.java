package chap6;

/**
 * 第37条：用标记接口定义类型。
 *
 * 1）标记接口是指没有包含方法声明的接口，用于只是指明一个类实现了具有某种属性的接口（比如Serializable）。
 * 2）如果标记是应用到任何程序元素而不是类或接口，就必须使用注解，因为只有类和接口可以用来实现或者扩展接口。
 * 3）标记接口和标记注解各有各的用处。如果主要用在类上，就考虑使用标记接口；如果用在成员或方法上，就考虑用标记注解。
 */
public class Law37 {
}
